/**
 * mcp-ariel-memory — npm wrapper
 *
 * This package installs and runs the Python MCP server.
 * Requires Python 3.10+ on the system.
 *
 * Usage:
 *   npx mcp-ariel-memory --transport stdio
 *   npx mcp-ariel-memory --transport http --port 8000
 */
module.exports = {};
